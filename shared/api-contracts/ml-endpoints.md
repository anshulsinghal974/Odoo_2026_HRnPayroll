# ML Endpoints Contract
> Owned by Person 3. Consumed by backend's `ml-client.service.ts`.

Person 3 will document all ML service request/response shapes here.
Backend will build `ml-client.service.ts` against these contracts.

## Endpoints (to be filled by Person 3)

<!-- Example format:
### POST /api/warnings/payrun-readiness
**Request:** `{ payrunId: string }`
**Response:** `{ readinessScore: number, warnings: [...] }`
-->
