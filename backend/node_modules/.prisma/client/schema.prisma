generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Dummy model to allow Prisma Client generation during scaffolding
model Dummy {
  id Int @id @default(autoincrement())
}
